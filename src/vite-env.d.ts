// Vite's ambient types: declarations for asset imports (*.css, *.svg, ?raw,
// ?url, ...), import.meta.env, and the HMR API. The triple-slash reference
// works even though tsconfig sets "types": [] — that only gates automatic
// @types/* inclusion, not explicit references.
/// <reference types="vite/client" />
